services:
  simple-spaceapi:
    build: .
    ports:
      - "1337:1337"
    volumes:
      - ./data/base:/app/data/base
      - ./data/uploads:/app/public/uploads
    env_file:
      - .env
    restart: unless-stopped
